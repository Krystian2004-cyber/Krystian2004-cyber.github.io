{{ form_start(form) }}
    <div class="card mb-3">
        <div class="card-body">
            {{ form_widget(form) }}
            <button class="btn btn-primary mt-3">{{ button_label|default('Save') }}</button>
        </div>
    </div>
{{ form_end(form) }}
